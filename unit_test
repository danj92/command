// run unit test
ng test --watch

// this ignore
xit
// run used this test
fit

// update jasmin
npm install @types/jasmine@latest karma-jasmine@latest karma-jasmine-html-reporter@latest jasmine-core@latest jasmine-spec-reporter@latest
---------
component['makeDefaultForm'](); //privaste -test czarnej skrzynki
AttachmentFormComponent.makeAttachmentForm() //static
--------
spyOn(component, 'register').and.callThrough();
and.callThrough() - шпион действует как прокси-сервер, вызывая реальную функцию, но проходя через объект-шпион, позволяя добавлять тесты, такие как ожидание

spyOn(foo, 'getBar').andCallThrough(); // продолжаем выполнение основной функции
spyOn(foo, 'getBar').andReturn(745); // подменяем возвращаемое значение
spyOn(foo, 'getBar').andCallFake(function() {return 1001;}); // подменяем метод
