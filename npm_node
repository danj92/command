// SSH
ssh-keygen
cat .ssh/id_rsa.pub
copy this key in to github

// install curl
sudo apt-get install curl

-------------------------------------------
// remove nodejs
sudo apt-get purge --auto-remove nodejs
sudo apt-get purge nodejs
sudo apt-get autoremove

// install node/(nmp)
curl -sL https://deb.nodesource.com/setup_11.x | sudo -E bash -
sudo apt-get install -y nodejs
// instal n (change version node)
sudo npm install -g n

// Upgrade npm to the most recent release:
npm install -g npm@next

// CLI
sudo npm install -g @angular/cli@latest
npm install node-sass

//login like root
sudo su



-------------NVM------------

// install NVM (restart console)
wget -qO- https://raw.githubusercontent.com/creationix/nvm/v0.33.11/install.sh | bash
nvm install 11.6.0

// put in .zshrc (work with nvm)
export NVM_DIR="~/.nvm"
source ~/.nvm/nvm.sh

// remove nvm
rm -rf $NVM_DIR ~/.npm ~/.bower
rm -rf $NVM_DIR
nvm uninstall-nvm
sudo rm -rf ~/.nvm



----------------/////----------------------

// SSH
ssh-keygen
cat .ssh/id_rsa.pub
copy this key in to github

// install curl
sudo apt-get install curl

-------------------------------------------
// remove nodejs
sudo apt-get purge --auto-remove nodejs
sudo apt-get purge nodejs
sudo apt-get autoremove

// install node/(nmp)
curl -sL https://deb.nodesource.com/setup_11.x | sudo -E bash -
sudo apt-get install -y nodejs
// instal n (change version node)
sudo npm install -g n

// Upgrade npm to the most recent release:
npm install -g npm@next

// CLI
sudo npm install -g @angular/cli@latest
npm install node-sass

//login like root
sudo su



-------------NVM------------

// install NVM (restart console)
wget -qO- https://raw.githubusercontent.com/creationix/nvm/v0.33.11/install.sh | bash
nvm install 11.6.0

// put in .zshrc (work with nvm)
export NVM_DIR="~/.nvm"
source ~/.nvm/nvm.sh

// remove nvm
rm -rf $NVM_DIR ~/.npm ~/.bower
rm -rf $NVM_DIR
nvm uninstall-nvm
sudo rm -rf ~/.nvm





------------------------------------------PYTHON-------------------------
// check version python
ls -ls /usr/bin/python*

▶ sudo apt-get install python3.7

The following additional packages will be installed:
  libpython3.7-minimal libpython3.7-stdlib python3.7-minimal
Suggested packages:
  python3.7-venv python3.7-doc binfmt-support
The following NEW packages will be installed:
  libpython3.7-minimal libpython3.7-stdlib python3.7 python3.7-minimal


▶ sudo apt-get install python3.7-venv

The following additional packages will be installed:
  python-pip-whl
The following NEW packages will be installed:
  python-pip-whl python3.7-venv

?????????
▶ sudo apt-get install python3-venv

The following additional packages will be installed:
  python3-distutils python3-lib2to3 python3.6-venv
The following NEW packages will be installed:
  python3-distutils python3-lib2to3 python3-venv python3.6-venv

// delete  /tmp/agralan--requirements.txt


▶ sudo apt-get install python3.7-dev

The following additional packages will be installed:
  libpython3.7 libpython3.7-dev
The following NEW packages will be installed:
  libpython3.7 libpython3.7-dev python3.7-dev


▶ sudo apt-get install redis-server
The following additional packages will be installed:
  libjemalloc1 redis-tools
Suggested packages:
  ruby-redis
The following NEW packages will be installed:
  libjemalloc1 redis-server redis-tools

▶ sudo systemctl status redis-serve
▶ sudo systemctl enable redis
▶ sudo systemctl start redis


sudo -u postgres bash -c "createuser $USER --superuser --login"
createdb $USER
bin/resetdb
backend/bin/serve

sudo apt-get install --reinstall cinnamon-control-center
gnom-control-center

