// proviryty versiju
▶ bin/getversion
0.5.0 0.6.0

// make prodaction with settings
ng build --prod --configuration=aws.production

// dodaje versiu a jak nie to  exit
▶  bin/setversion

// wydalenia tagu 
▶ git tag -d 0.6.0

// podywytys szo wijszlo 
git tag -n9

// run deploy backend
▶ bin/deployall staging 2>&1 | tee ~/staging-release.log

// zapusk project zi zminnoju
▶ DJANGO_SETTINGS_MODULE=settings.e2e backend/bin/serve

// run sigle test
pytest -sk offers/tests/test_api.py:test_my_offer_details

//run all test
pytest

-------------ssh----------
ssh tul@192.168.1.1
yes
password
sudo su jenkins
password
cd ~/
ls
cd workspace/
cd workspace/pull-request-build
cd frontend/
rm -R node_modules/
-----------------------------

// check version redis
▶ redis-cli
127.0.0.1:6379> keys *
